import styles from "./";

export default function BotaoAdicionarPerg() {
  return (
    <div>
      <button className={`${styles["salvar-mudancas"]} doodle-border`}>
        Criar Quizz
      </button>
    </div>
  );
}
